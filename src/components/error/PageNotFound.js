/** @format */

import React from "react";
import ErrorIcon from "@material-ui/icons/Error";

const PageNotFound = () => {
  return (
    <div>
      <ErrorIcon />
    </div>
  );
};

export default PageNotFound;
